<view class="container">
  <block wx:if="{{key == 'notice'}}">
    <view class="title">{{notice.title}}</view>
  </block>
  <block wx:else>
    <parser content="{{notice}}" domain="{{doMain}}"></parser>
  </block>
</view>