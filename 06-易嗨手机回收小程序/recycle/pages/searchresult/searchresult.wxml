<block wx:if="{{result.length>0}}">
	<view class="list" wx:for="{{result}}" wx:key="index" data-name="{{item.name}}" data-id="{{item.id}}" bindtap="getEvaluation">
		{{item.name}}
	</view>
</block>
<block wx:else>
	<view class="t-a-c list">
		暂无数据
	</view>
</block>